# HW artefacts — folder layout

Paths from repo root. Commits with issues: [GlobalReport.md](../../documentation/group/GlobalReport.md) §2.1.

| Path | What goes here |
|------|----------------|
| `ai-images/<Sx>/` | AI first pass, edited final, optional `prompts.md`. |
| `mockups/<Sx>/` | Mug/poster/etc. exports. |
| `presentations/<Sx>/` | Sources + English PDF; two-tool comparison stays in the individual report. |
| `selected/` | After the group vote: winning image set and winning PDF. |

`<Sx>` is `S1`–`S4` like in the team table.

When you vote: write the result in GlobalReport §5 and copy files into `selected/motivational/` and `selected/presentation/`.
