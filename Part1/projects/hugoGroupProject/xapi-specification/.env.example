# Copy to .env (gitignored). Mirror the same keys in Netlify → Site settings → Environment variables.
# Group decision: one shared lrs.io instance for production; all Twine stories use the same triple.
XAPI_LRS_ENDPOINT=
XAPI_LRS_USERNAME=
XAPI_LRS_PASSWORD=
